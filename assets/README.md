# Asset bundle storage

Place `asset_bundle.zip` in this directory if you want the **Quality Gates** workflow to use a locally checked-in
bundle instead of downloading from a URL. The workflow reads the `asset_path` input (default:
`assets/asset_bundle.zip`) when `asset_url` is left empty.
